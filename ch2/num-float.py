print(0.2 + 0.1)

# Python tries to find a way to represent 
# the result as precisely as possible, 
# which is sometimes difficult given 
# how computers have to represent numbers internally.



#If you mix an integer and a float in any other operation, you’ll get a float as well:
print(1 + 2.0) # 3.0


# multiple assignment
# Python will assign each value to its respectively positioned variable
# ValueError will occur if there are more/less values or variable than expected
x, y, z = 0, 0, 0

print(x)
print(y)
print(z)